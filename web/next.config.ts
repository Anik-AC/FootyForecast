import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The Go API URL is read by server components at render time.
  // Set API_URL in .env.local for development, or as an env var in production.
};

export default nextConfig;
